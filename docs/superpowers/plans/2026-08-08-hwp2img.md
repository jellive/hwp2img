# hwp2img Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 어머니가 드래그앤드롭 한 번으로 `.hwp` 문서를 카카오톡에 바로 보낼 수 있는 이미지로 바꿔주는 Windows 프로그램을 만든다.

**Architecture:** pyhwpx(한글 COM 자동화)로 hwp→PDF 저장(공식 `SaveAs(path, "PDF")` API) → PyMuPDF로 PDF 각 페이지를 PNG 렌더 → 여러 페이지면 Pillow로 세로 1장으로 합성 → 바탕화면 고정 폴더에 저장 + 클립보드 복사 + 탐색기 자동 오픈. PyInstaller로 단일 `.exe`.

**Tech Stack:** Python 3.11+, pyhwpx, PyMuPDF, Pillow, pywin32, PyInstaller, pytest

## Global Constraints

- 소스 레이아웃: `src/hwp2img/` 패키지, 테스트는 `tests/`. pytest는 `pyproject.toml`의 `pythonpath = ["src"]`로 이 레이아웃을 인식한다.
- Windows 전용 의존성(`pyhwpx`, `pywin32`)은 **반드시 함수 내부에서 지연 임포트**한다 — 모듈 자체는 Mac에서도 임포트/테스트 가능해야 한다 (Task 6, 7).
- Windows 전용 객체(한글 COM 인스턴스, 클립보드 모듈)는 **항상 의존성 주입**으로 넘겨 테스트 가능하게 만든다 (Task 5, 6, 7의 Fake 패턴).
- 출력 파일명 규칙: `<원본 hwp 파일명(확장자 제외)>_변환.png`.
- 출력 폴더: ~~어머니 계정의 `~/Desktop/변환된사진/` (Windows 사용자 홈 기준)~~ **최종 리뷰 후 변경**:
  원본 `.hwp` 파일과 같은 폴더. OneDrive 데스크톱 동기화 환경에서는 `~/Desktop` 고정 경로가
  실제 바탕화면과 어긋나 `os.makedirs` 가 유령 폴더를 만드는 문제가 있어, 아래 Task 4/5/7 의
  코드 스니펫(작성 당시 기준)과 달리 최종 구현은 원본 파일 옆 저장 + 탐색기 `/select` 로
  바뀌었다.
- 여러 페이지 문서는 **세로로 이어붙여 파일 1개로만** 출력한다 — 페이지별 개별 파일 옵션은 스코프 아웃(스펙 확정 사항, 혼란 방지).
- 사용자에게 보이는 에러 메시지는 항상 한국어, 쉬운 말로 쓴다 (`errors.py`의 `user_message`).
- 스코프 아웃: macOS/Linux 지원, 자동 업데이트, 배치(다중 파일 동시 드롭), GUI 설정 화면 — 전부 만들지 않는다.
- ~~`pyhwpx.Hwp`는 기본 생성자에서 `register_module=True`가 기본값이라 보안승인모듈을 자동 등록한다 — 우리 코드에서 레지스트리를 직접 건드리지 않는다.~~
  **Task 9 실측 후 철회**: pyhwpx 의 자동 등록은 dll 위치를 찾으려고 `pip show pyhwpx` 를
  서브프로세스로 부른다. 얼린 exe 에는 pip 가 없어 그 호출이 실패하고, 실패를 삼킨 뒤
  초기화 안 된 지역변수를 읽어 `UnboundLocalError` 로 죽는다. 그러면 이어지는
  `RegisterModule()` 호출까지 통째로 건너뛴다. 개발 PC 에서도 (venv 의 `pip` 가 PATH 에
  없으면) 똑같이 실패하지만 한글이 팝업을 안 띄우면 그냥 지나간다 — 어머니 PC 에서만
  드러날 수 있는 종류의 실패다. 그래서 `security.py` 에서 dll 을 직접 찾아 HKCU 에 등록한다.

---

### Task 1: 프로젝트 스캐폴딩 + 에러 타입

**Files:**
- Create: `requirements.txt`
- Create: `pyproject.toml`
- Create: `src/hwp2img/__init__.py`
- Create: `src/hwp2img/errors.py`
- Test: `tests/test_errors.py`

**Interfaces:**
- Produces: `Hwp2ImgError` (base), `UnsupportedFileError(path: str)`, `HwpNotInstalledError(detail: str = "")`, `HwpAutomationError(detail: str = "")` — 전부 `.user_message: str` 속성을 가짐

- [ ] **Step 1: requirements.txt 작성**

```
PyMuPDF
Pillow
pytest

pyhwpx; sys_platform == "win32"
pywin32; sys_platform == "win32"
```

- [ ] **Step 2: pyproject.toml 작성**

```toml
[project]
name = "hwp2img"
version = "0.1.0"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["src"]
```

- [ ] **Step 3: 가상환경 생성 + 의존성 설치**

Run: `python3 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt`

- [ ] **Step 4: 실패하는 테스트 작성**

```python
# tests/test_errors.py
from hwp2img.errors import Hwp2ImgError, UnsupportedFileError, HwpNotInstalledError, HwpAutomationError


def test_unsupported_file_error_has_korean_message():
    exc = UnsupportedFileError("notes.txt")
    assert exc.user_message == "한글 문서 파일(.hwp, .hwpx)만 변환할 수 있어요."
    assert isinstance(exc, Hwp2ImgError)


def test_hwp_not_installed_error_has_korean_message():
    exc = HwpNotInstalledError("no pyhwpx")
    assert "한글이 설치되어 있는지" in exc.user_message


def test_hwp_automation_error_has_korean_message():
    exc = HwpAutomationError("open failed")
    assert "손상되지" in exc.user_message
```

- [ ] **Step 5: 테스트 실패 확인**

Run: `pytest tests/test_errors.py -v`
Expected: FAIL with `ModuleNotFoundError: No module named 'hwp2img'`

- [ ] **Step 6: 구현**

```python
# src/hwp2img/__init__.py
```
(빈 파일)

```python
# src/hwp2img/errors.py
class Hwp2ImgError(Exception):
    user_message = "변환 중 문제가 생겼어요."


class UnsupportedFileError(Hwp2ImgError):
    def __init__(self, path: str):
        super().__init__(f"unsupported file: {path}")
        self.user_message = "한글 문서 파일(.hwp, .hwpx)만 변환할 수 있어요."


class HwpNotInstalledError(Hwp2ImgError):
    def __init__(self, detail: str = ""):
        super().__init__(detail)
        self.user_message = "한글 프로그램을 찾을 수 없어요. 컴퓨터에 한글이 설치되어 있는지 확인해 주세요."


class HwpAutomationError(Hwp2ImgError):
    def __init__(self, detail: str = ""):
        super().__init__(detail)
        self.user_message = "한글 문서를 여는 중 문제가 생겼어요. 파일이 손상되지 않았는지 확인해 주세요."
```

- [ ] **Step 7: 테스트 통과 확인**

Run: `pytest tests/test_errors.py -v`
Expected: PASS (3 tests)

- [ ] **Step 8: 커밋**

```bash
git add requirements.txt pyproject.toml src/hwp2img/__init__.py src/hwp2img/errors.py tests/test_errors.py
git commit -m "feat: 프로젝트 스캐폴딩 + 에러 타입"
```

---

### Task 2: PDF → 이미지 렌더

**Files:**
- Create: `tests/conftest.py`
- Create: `tests/test_pdf_to_images.py`
- Create: `src/hwp2img/pdf_to_images.py`

**Interfaces:**
- Produces: `render_pages(pdf_path: str, dpi: int = 200) -> list[PIL.Image.Image]`
- Produces (테스트 인프라): `make_pdf` pytest fixture — `make_pdf(page_count: int, width: int = 200, height: int = 300) -> str` (생성된 PDF 경로)

- [ ] **Step 1: 공용 PDF 픽스처 작성**

```python
# tests/conftest.py
import pymupdf
import pytest


@pytest.fixture
def make_pdf(tmp_path):
    """factory fixture: make_pdf(page_count) -> 그 페이지 수만큼 빈 페이지가 있는 PDF 경로"""

    def _make(page_count: int, width: int = 200, height: int = 300) -> str:
        path = tmp_path / f"sample_{page_count}page.pdf"
        doc = pymupdf.open()
        for i in range(page_count):
            page = doc.new_page(width=width, height=height)
            page.insert_text((10, 10), f"page {i + 1}")
        doc.save(str(path))
        doc.close()
        return str(path)

    return _make
```

- [ ] **Step 2: 실패하는 테스트 작성**

```python
# tests/test_pdf_to_images.py
from hwp2img.pdf_to_images import render_pages


def test_render_pages_returns_one_image_per_page(make_pdf):
    pdf_path = make_pdf(page_count=3)

    images = render_pages(pdf_path)

    assert len(images) == 3


def test_render_pages_respects_dpi(make_pdf):
    pdf_path = make_pdf(page_count=1, width=200, height=300)

    images_low = render_pages(pdf_path, dpi=72)
    images_high = render_pages(pdf_path, dpi=200)

    assert images_high[0].width > images_low[0].width
    assert images_high[0].height > images_low[0].height
```

- [ ] **Step 3: 테스트 실패 확인**

Run: `pytest tests/test_pdf_to_images.py -v`
Expected: FAIL with `ModuleNotFoundError: No module named 'hwp2img.pdf_to_images'`

- [ ] **Step 4: 구현**

```python
# src/hwp2img/pdf_to_images.py
import pymupdf
from PIL import Image


def render_pages(pdf_path: str, dpi: int = 200) -> list[Image.Image]:
    doc = pymupdf.open(pdf_path)
    try:
        return [page.get_pixmap(dpi=dpi).pil_image() for page in doc]
    finally:
        doc.close()
```

- [ ] **Step 5: 테스트 통과 확인**

Run: `pytest tests/test_pdf_to_images.py -v`
Expected: PASS (2 tests)

- [ ] **Step 6: 커밋**

```bash
git add tests/conftest.py tests/test_pdf_to_images.py src/hwp2img/pdf_to_images.py
git commit -m "feat: PDF 페이지를 PNG로 렌더링"
```

---

### Task 3: 여러 페이지 세로 합성

**Files:**
- Create: `tests/test_stitch.py`
- Create: `src/hwp2img/stitch.py`

**Interfaces:**
- Consumes: `PIL.Image.Image` 리스트 (Task 2의 `render_pages` 출력과 동일한 타입)
- Produces: `stitch_vertical(images: list[PIL.Image.Image]) -> PIL.Image.Image`

- [ ] **Step 1: 실패하는 테스트 작성**

```python
# tests/test_stitch.py
import pytest
from PIL import Image

from hwp2img.stitch import stitch_vertical


def test_stitch_vertical_stacks_images_top_to_bottom():
    img1 = Image.new("RGB", (100, 50), color="white")
    img2 = Image.new("RGB", (80, 60), color="black")

    result = stitch_vertical([img1, img2])

    assert result.width == 100
    assert result.height == 110
    assert result.getpixel((10, 10)) == (255, 255, 255)  # img1 영역
    assert result.getpixel((10, 70)) == (0, 0, 0)  # img2 영역


def test_stitch_vertical_single_image_passthrough():
    img = Image.new("RGB", (50, 50), color="red")

    result = stitch_vertical([img])

    assert result is img


def test_stitch_vertical_empty_list_raises():
    with pytest.raises(ValueError):
        stitch_vertical([])
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `pytest tests/test_stitch.py -v`
Expected: FAIL with `ModuleNotFoundError: No module named 'hwp2img.stitch'`

- [ ] **Step 3: 구현**

```python
# src/hwp2img/stitch.py
from PIL import Image


def stitch_vertical(images: list[Image.Image]) -> Image.Image:
    if not images:
        raise ValueError("images must not be empty")
    if len(images) == 1:
        return images[0]

    width = max(img.width for img in images)
    total_height = sum(img.height for img in images)
    canvas = Image.new("RGB", (width, total_height), color="white")

    y = 0
    for img in images:
        canvas.paste(img, (0, y))
        y += img.height
    return canvas
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `pytest tests/test_stitch.py -v`
Expected: PASS (3 tests)

- [ ] **Step 5: 커밋**

```bash
git add tests/test_stitch.py src/hwp2img/stitch.py
git commit -m "feat: 여러 페이지를 세로 1장으로 합성"
```

---

### Task 4: 결과 이미지 저장

**Files:**
- Create: `tests/test_output.py`
- Create: `src/hwp2img/output.py`

**Interfaces:**
- Produces: `save_result(image: PIL.Image.Image, source_hwp_path: str, output_dir: str) -> str` (저장된 전체 경로 반환)

- [ ] **Step 1: 실패하는 테스트 작성**

```python
# tests/test_output.py
from PIL import Image

from hwp2img.output import save_result


def test_save_result_writes_png_named_after_source(tmp_path):
    image = Image.new("RGB", (10, 10), color="white")
    output_dir = tmp_path / "변환된사진"

    out_path = save_result(image, "/원본/공문_2026.hwp", str(output_dir))

    assert out_path == str(output_dir / "공문_2026_변환.png")
    assert (output_dir / "공문_2026_변환.png").exists()


def test_save_result_creates_output_dir_if_missing(tmp_path):
    image = Image.new("RGB", (10, 10), color="white")
    output_dir = tmp_path / "does" / "not" / "exist"

    save_result(image, "doc.hwp", str(output_dir))

    assert output_dir.is_dir()
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `pytest tests/test_output.py -v`
Expected: FAIL with `ModuleNotFoundError: No module named 'hwp2img.output'`

- [ ] **Step 3: 구현**

```python
# src/hwp2img/output.py
import os
from pathlib import Path

from PIL import Image


def save_result(image: Image.Image, source_hwp_path: str, output_dir: str) -> str:
    os.makedirs(output_dir, exist_ok=True)
    stem = Path(source_hwp_path).stem
    out_path = os.path.join(output_dir, f"{stem}_변환.png")
    image.save(out_path, "PNG")
    return out_path
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `pytest tests/test_output.py -v`
Expected: PASS (2 tests)

- [ ] **Step 5: 커밋**

```bash
git add tests/test_output.py src/hwp2img/output.py
git commit -m "feat: 변환 결과를 PNG로 저장"
```

---

### Task 5: 클립보드 복사 + 탐색기 자동 오픈

**Files:**
- Modify: `tests/test_output.py` (테스트 추가)
- Modify: `src/hwp2img/output.py` (함수 추가)

**Interfaces:**
- Produces: `copy_to_clipboard(image: PIL.Image.Image, clipboard_module=None) -> None`
- Produces: `open_in_explorer(path: str, runner=subprocess.run) -> None`
- 둘 다 Windows 전용 동작이라 실제 OS 검증은 Task 9(원격 세션)에서 한다. 여기서는 **우리 쪽 로직**(BMP 헤더 스트리핑, CF_DIB 사용, explorer 인자 형식)만 Fake로 검증한다.

- [ ] **Step 1: 실패하는 테스트 작성**

```python
# tests/test_output.py 에 추가
from hwp2img.output import copy_to_clipboard, open_in_explorer


class FakeClipboard:
    CF_DIB = 8

    def __init__(self, raise_on_set=False):
        self.calls = []
        self._raise_on_set = raise_on_set

    def OpenClipboard(self):
        self.calls.append("open")

    def EmptyClipboard(self):
        self.calls.append("empty")

    def SetClipboardData(self, fmt, data):
        self.calls.append(("set", fmt, data))
        if self._raise_on_set:
            raise RuntimeError("boom")

    def CloseClipboard(self):
        self.calls.append("close")


def test_copy_to_clipboard_strips_bmp_header_and_uses_cf_dib():
    image = Image.new("RGB", (10, 10), color="blue")
    fake = FakeClipboard()

    copy_to_clipboard(image, clipboard_module=fake)

    assert fake.calls[0] == "open"
    assert fake.calls[1] == "empty"
    set_call = fake.calls[2]
    assert set_call[0] == "set"
    assert set_call[1] == FakeClipboard.CF_DIB
    assert not set_call[2].startswith(b"BM")  # BMP 파일 헤더(14바이트)가 제거됐어야 함
    assert fake.calls[3] == "close"


def test_copy_to_clipboard_closes_even_if_set_fails():
    image = Image.new("RGB", (10, 10), color="blue")
    fake = FakeClipboard(raise_on_set=True)

    with pytest.raises(RuntimeError):
        copy_to_clipboard(image, clipboard_module=fake)

    assert fake.calls[-1] == "close"


def test_open_in_explorer_selects_the_file():
    calls = []

    open_in_explorer(
        "C:\\Users\\mom\\Desktop\\변환된사진\\문서_변환.png",
        runner=lambda args: calls.append(args),
    )

    assert calls == [["explorer", "/select,C:\\Users\\mom\\Desktop\\변환된사진\\문서_변환.png"]]
```

(파일 상단에 `import pytest` 가 없다면 추가할 것)

- [ ] **Step 2: 테스트 실패 확인**

Run: `pytest tests/test_output.py -v`
Expected: FAIL with `ImportError: cannot import name 'copy_to_clipboard'`

- [ ] **Step 3: 구현**

```python
# src/hwp2img/output.py 에 추가
import io
import subprocess


def copy_to_clipboard(image: Image.Image, clipboard_module=None) -> None:
    if clipboard_module is None:
        import win32clipboard

        clipboard_module = win32clipboard

    buffer = io.BytesIO()
    image.convert("RGB").save(buffer, "BMP")
    data = buffer.getvalue()[14:]  # BMP 파일 헤더(14바이트)는 CF_DIB 에 포함되지 않는다
    buffer.close()

    clipboard_module.OpenClipboard()
    try:
        clipboard_module.EmptyClipboard()
        clipboard_module.SetClipboardData(clipboard_module.CF_DIB, data)
    finally:
        clipboard_module.CloseClipboard()


def open_in_explorer(path: str, runner=subprocess.run) -> None:
    runner(["explorer", f"/select,{path}"])
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `pytest tests/test_output.py -v`
Expected: PASS (5 tests total in this file)

- [ ] **Step 5: 커밋**

```bash
git add tests/test_output.py src/hwp2img/output.py
git commit -m "feat: 결과 이미지 클립보드 복사 + 탐색기 자동 오픈"
```

---

### Task 6: HWP → PDF 변환 (한글 COM 자동화)

**Files:**
- Create: `tests/test_hwp_to_pdf.py`
- Create: `src/hwp2img/hwp_to_pdf.py`

**Interfaces:**
- Produces: `convert(hwp, hwp_path: str, pdf_path: str) -> None` — `hwp`는 `open(filename) -> bool`, `save_as(path, format="HWP") -> bool`, `quit(save=False) -> None` 을 갖는 객체(운영에서는 `pyhwpx.Hwp` 인스턴스, 테스트에서는 Fake)
- Produces: `create_hwp()` — 실제 `pyhwpx.Hwp(visible=False)` 인스턴스를 만드는 팩토리. **이 함수 자체는 Windows + 한컴오피스 없이는 동작 검증 불가 — Task 9에서 수동 검증**
- Consumes: `HwpAutomationError`, `HwpNotInstalledError` (Task 1)

- [ ] **Step 1: 실패하는 테스트 작성**

```python
# tests/test_hwp_to_pdf.py
import pytest

from hwp2img.errors import HwpAutomationError
from hwp2img.hwp_to_pdf import convert


class FakeHwp:
    def __init__(self, open_result=True, save_result=True):
        self.calls = []
        self._open_result = open_result
        self._save_result = save_result

    def open(self, filename):
        self.calls.append(("open", filename))
        return self._open_result

    def save_as(self, path, format="HWP"):
        self.calls.append(("save_as", path, format))
        return self._save_result

    def quit(self, save=False):
        self.calls.append(("quit", save))


def test_convert_happy_path_opens_saves_and_quits():
    hwp = FakeHwp()

    convert(hwp, "문서.hwp", "출력.pdf")

    assert hwp.calls == [
        ("open", "문서.hwp"),
        ("save_as", "출력.pdf", "PDF"),
        ("quit", False),
    ]


def test_convert_raises_when_open_fails_but_still_quits():
    hwp = FakeHwp(open_result=False)

    with pytest.raises(HwpAutomationError):
        convert(hwp, "문서.hwp", "출력.pdf")

    assert ("quit", False) in hwp.calls


def test_convert_raises_when_save_fails_but_still_quits():
    hwp = FakeHwp(save_result=False)

    with pytest.raises(HwpAutomationError):
        convert(hwp, "문서.hwp", "출력.pdf")

    assert ("quit", False) in hwp.calls
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `pytest tests/test_hwp_to_pdf.py -v`
Expected: FAIL with `ModuleNotFoundError: No module named 'hwp2img.hwp_to_pdf'`

- [ ] **Step 3: 구현**

```python
# src/hwp2img/hwp_to_pdf.py
from hwp2img.errors import HwpAutomationError, HwpNotInstalledError


def convert(hwp, hwp_path: str, pdf_path: str) -> None:
    try:
        if not hwp.open(hwp_path):
            raise HwpAutomationError(f"failed to open {hwp_path}")
        if not hwp.save_as(pdf_path, format="PDF"):
            raise HwpAutomationError(f"failed to save {pdf_path} as PDF")
    finally:
        hwp.quit(save=False)


def create_hwp():
    """실제 한글 COM 객체를 생성한다.
    Windows + 한컴오피스가 있어야 동작하므로 이 함수 자체는 원격 세션에서 수동으로 검증한다
    (계획 문서 Task 9 참고). pyhwpx 는 Hwp(register_module=True) 기본값으로 보안승인모듈을
    자동 등록하므로 여기서 레지스트리를 직접 건드리지 않는다.
    """
    try:
        from pyhwpx import Hwp
    except ImportError as exc:
        raise HwpNotInstalledError("pyhwpx not available") from exc

    try:
        return Hwp(visible=False)
    except Exception as exc:
        raise HwpNotInstalledError(str(exc)) from exc
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `pytest tests/test_hwp_to_pdf.py -v`
Expected: PASS (3 tests)

- [ ] **Step 5: 커밋**

```bash
git add tests/test_hwp_to_pdf.py src/hwp2img/hwp_to_pdf.py
git commit -m "feat: 한글 COM 자동화로 hwp를 PDF로 저장"
```

---

### Task 7: 파이프라인 조합 + 엔트리포인트

**Files:**
- Create: `tests/test_cli.py`
- Create: `src/hwp2img/cli.py`
- Create: `src/run.py`

**Interfaces:**
- Consumes: `hwp_to_pdf.convert`, `hwp_to_pdf.create_hwp` (Task 6), `pdf_to_images.render_pages` (Task 2), `stitch.stitch_vertical` (Task 3), `output.save_result` / `output.copy_to_clipboard` / `output.open_in_explorer` (Task 4, 5), `Hwp2ImgError` / `UnsupportedFileError` (Task 1)
- Produces: `process_file(hwp_path: str, output_dir: str, hwp_factory, dpi: int = 200) -> str`
- Produces: `main(argv: list[str]) -> int`

- [ ] **Step 1: 실패하는 테스트 작성**

```python
# tests/test_cli.py
import pymupdf
import pytest
from PIL import Image

from hwp2img import cli
from hwp2img.errors import UnsupportedFileError


class FakeHwpWritesRealPdf:
    """save_as 호출 시 실제로 읽을 수 있는 PDF 를 만들어 이후 파이프라인(렌더링)이
    진짜로 동작하는지까지 확인할 수 있게 한다."""

    def __init__(self, page_count=1):
        self.page_count = page_count
        self.calls = []

    def open(self, filename):
        self.calls.append(("open", filename))
        return True

    def save_as(self, path, format="HWP"):
        doc = pymupdf.open()
        for i in range(self.page_count):
            page = doc.new_page(width=200, height=300)
            page.insert_text((10, 10), f"page {i + 1}")
        doc.save(path)
        doc.close()
        self.calls.append(("save_as", path, format))
        return True

    def quit(self, save=False):
        self.calls.append(("quit", save))


def test_process_file_rejects_unsupported_extension(tmp_path):
    def factory():
        raise AssertionError("hwp_factory should not be called for unsupported files")

    with pytest.raises(UnsupportedFileError):
        cli.process_file(str(tmp_path / "notes.txt"), str(tmp_path / "out"), factory)


def test_process_file_produces_stitched_png(tmp_path, monkeypatch):
    clipboard_calls = []
    explorer_calls = []
    monkeypatch.setattr(cli.output, "copy_to_clipboard", lambda image: clipboard_calls.append(image))
    monkeypatch.setattr(cli.output, "open_in_explorer", lambda path: explorer_calls.append(path))

    hwp_path = tmp_path / "공문.hwp"
    hwp_path.write_text("dummy")  # 내용은 FakeHwp 가 무시하므로 존재만 하면 됨
    output_dir = tmp_path / "out"

    out_path = cli.process_file(
        str(hwp_path), str(output_dir), lambda: FakeHwpWritesRealPdf(page_count=2)
    )

    assert out_path == str(output_dir / "공문_변환.png")
    assert (output_dir / "공문_변환.png").exists()
    assert len(clipboard_calls) == 1
    assert explorer_calls == [out_path]


def test_main_shows_error_and_returns_1_when_no_files(monkeypatch):
    messages = []
    monkeypatch.setattr(cli, "_show_error", lambda msg: messages.append(msg))

    result = cli.main([])

    assert result == 1
    assert messages


def test_main_shows_user_message_on_hwp2img_error(monkeypatch):
    messages = []
    monkeypatch.setattr(cli, "_show_error", lambda msg: messages.append(msg))

    def failing_process_file(*args, **kwargs):
        raise UnsupportedFileError("bad.txt")

    monkeypatch.setattr(cli, "process_file", failing_process_file)

    result = cli.main(["bad.txt"])

    assert result == 1
    assert messages == ["한글 문서 파일(.hwp, .hwpx)만 변환할 수 있어요."]
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `pytest tests/test_cli.py -v`
Expected: FAIL with `ModuleNotFoundError: No module named 'hwp2img.cli'`

- [ ] **Step 3: 구현**

```python
# src/hwp2img/cli.py
import os
import tempfile
from pathlib import Path

from hwp2img import hwp_to_pdf, output, pdf_to_images, stitch
from hwp2img.errors import Hwp2ImgError, UnsupportedFileError

SUPPORTED_EXTENSIONS = {".hwp", ".hwpx"}


def process_file(hwp_path: str, output_dir: str, hwp_factory, dpi: int = 200) -> str:
    if Path(hwp_path).suffix.lower() not in SUPPORTED_EXTENSIONS:
        raise UnsupportedFileError(hwp_path)

    with tempfile.TemporaryDirectory() as tmp:
        pdf_path = os.path.join(tmp, "converted.pdf")
        hwp = hwp_factory()
        hwp_to_pdf.convert(hwp, hwp_path, pdf_path)
        pages = pdf_to_images.render_pages(pdf_path, dpi=dpi)

    image = stitch.stitch_vertical(pages)
    out_path = output.save_result(image, hwp_path, output_dir)
    output.copy_to_clipboard(image)
    output.open_in_explorer(out_path)
    return out_path


def main(argv: list[str]) -> int:
    if not argv:
        _show_error("변환할 한글 문서 파일을 이 프로그램 위로 끌어다 놓아 주세요.")
        return 1

    output_dir = os.path.join(os.path.expanduser("~"), "Desktop", "변환된사진")
    for hwp_path in argv:
        try:
            process_file(hwp_path, output_dir, hwp_to_pdf.create_hwp)
        except Hwp2ImgError as exc:
            _show_error(exc.user_message)
            return 1
        except Exception as exc:
            _show_error(f"예상하지 못한 문제가 생겼어요.\n\n{exc}")
            return 1
    return 0


def _show_error(message: str) -> None:
    import ctypes

    ctypes.windll.user32.MessageBoxW(0, message, "한글 사진으로 바꾸기", 0x10)
```

```python
# src/run.py — PyInstaller 엔트리포인트
import sys

from hwp2img.cli import main

if __name__ == "__main__":
    sys.exit(main(sys.argv[1:]))
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `pytest tests/test_cli.py -v`
Expected: PASS (4 tests)

- [ ] **Step 5: 전체 테스트 스위트 확인**

Run: `pytest -v`
Expected: 이 시점까지 작성된 모든 테스트 PASS

- [ ] **Step 6: 커밋**

```bash
git add tests/test_cli.py src/hwp2img/cli.py src/run.py
git commit -m "feat: 전체 파이프라인 조합 + 드래그앤드롭 엔트리포인트"
```

---

### Task 8: PyInstaller 패키징 설정 + README

**Files:**
- Create: `README.md`

이 태스크는 코드가 아니라 **문서 + 빌드 설정**이라 자동 테스트가 없다. Windows 빌드 자체는
Mac에서 실행할 수 없으므로(PyInstaller는 크로스 컴파일 불가) 실제 검증은 Task 9(Windows 필요)에서 한다.

- [ ] **Step 1: README.md 작성**

```markdown
# hwp2img

어머니가 받은 한글(.hwp) 문서를 카카오톡으로 바로 보낼 수 있는 이미지로 바꿔주는 프로그램.

## 사용법 (어머니용)

1. 바탕화면의 "한글 사진으로 바꾸기" 아이콘 위로 변환하고 싶은 .hwp 파일을 끌어다 놓는다.
2. 잠시 기다리면 "변환된사진" 폴더가 자동으로 열린다.
3. 그 폴더의 사진을 카카오톡에 첨부해서 보내거나, 카카오톡 채팅창에 Ctrl+V 로 바로 붙여넣는다.

## 개발 환경 설정 (Mac/Windows 공통 — 순수 로직 테스트)

    python3 -m venv .venv
    source .venv/bin/activate  # Windows: .venv\Scripts\activate
    pip install -r requirements.txt
    pytest

`pyhwpx`/`pywin32`는 Windows 전용이라 Mac에서는 설치되지 않는다(`requirements.txt`의 플랫폼 마커).
`hwp_to_pdf.py`/`output.py`의 Windows 전용 함수는 의존성 주입으로 테스트되므로 Mac에서도 전체
테스트 스위트가 통과해야 한다.

## Windows 실기기 빌드 (반드시 Windows + 한컴오피스 설치 환경에서)

    pip install -r requirements.txt
    pip install pyinstaller
    pyinstaller --onefile --noconsole --name "한글사진으로바꾸기" src/run.py

결과물: `dist/한글사진으로바꾸기.exe`

## 최초 1회 배포 체크리스트

- [ ] 위 명령으로 만든 .exe 를 어머니 PC 바탕화면에 복사
- [ ] 실제 hwp 문서 하나로 드래그앤드롭 테스트 — 보안 팝업이 뜨는지 확인
      (pyhwpx 가 `Hwp(register_module=True)` 기본값으로 자동 등록하므로 이론상 안 떠야 함 — 실제로 확인할 것)
- [ ] "변환된사진" 폴더가 자동으로 열리는지, 결과 이미지가 원본과 비교해 읽을 수 있는 품질인지 확인
- [ ] 카카오톡에 Ctrl+V 로 붙여넣기가 실제로 되는지 확인
```

- [ ] **Step 2: 커밋**

```bash
git add README.md
git commit -m "docs: 사용법 + Windows 빌드 방법"
```

---

### Task 9: [수동 러너북 — Windows 접근 필요] 실기기 빌드 및 배포

**이 태스크는 TDD 대상이 아니다.** pyhwpx의 한글 COM 자동화, PyInstaller의 Windows 빌드,
클립보드/탐색기 동작은 전부 Windows + 한컴오피스가 실제로 있어야만 검증 가능하다. Jell은
Mac을 쓰고 어머니 PC는 원격/화면공유로만 접근 가능하므로, 이 태스크는 **Windows 머신(어머니
PC, VM, 또는 다른 Windows 장비)에 접근했을 때** 수행하는 체크리스트다.

**실행 기록: 2026-08-09, Windows 11 Pro / 한컴오피스 한글 9.0.0.562 / Python 3.11.**
아래 결과는 그 장비에서 실측한 것이다.

- [x] Windows 머신에서 저장소를 clone
- [x] README.md의 "Windows 실기기 빌드" 절 실행 → `.exe` 생성 (`dist/hwp2img.exe`, 62MB)
- [x] 실제 `.hwp` 파일(1페이지, 3페이지)로 실행 — 각 4초 내외, 원본 옆에 `_변환.png` 생성
- [x] 보안 팝업 안 뜨는지 확인 — **팝업은 안 떴지만 등록 자체가 실패하고 있었다.**
      매 실행마다 `RegisterModule 액션을 실행할 수 없음` 이 찍혔고 레지스트리에 값이 없었다.
      원인과 대응은 위 Global Constraints 참고. `security.py` 추가 후 오류 사라짐,
      exe 실행 시 번들된 `_MEIxxxx/pyhwpx/FilePathCheckerModule.dll` 로 자동 등록됨.
- [x] 결과 PNG 화질 확인 — `dpi=200` 에서 본문 글자가 또렷하다. 200 유지.
- [x] 여러 페이지 세로 합성 확인 — 3페이지 문서가 1653x7011 (2337 x 3) 로 정확히 이어진다.
      다만 페이지가 많아질수록 세로로 길어지므로 카카오톡 미리보기 잘림은 실사용에서 볼 것.
- [x] 클립보드 확인 — exe 실행 후 CF_DIB/CF_BITMAP/CF_DIBV5 가 모두 올라오고
      크기가 저장된 PNG 와 일치(1653x2337). 카카오톡 실붙여넣기는 배포 당일 확인.
- [ ] README.md의 "최초 1회 배포 체크리스트" 전부 실행 — 어머니 PC 에서 수행
- [ ] 위 항목이 전부 끝난 뒤에만 어머니 PC 바탕화면에 실제로 배치

#### Windows 에서만 확인 가능한 프로브 (최종 코드리뷰에서 지목된 항목)

Mac 테스트로는 도달할 수 없어서 여기 남긴다. 각 항목은 "무엇이 잘못되면 어떻게 보이는지"까지
확인해야 닫힌 것으로 친다.

- [x] **한글을 미리 띄워둔 상태에서 변환** — **PASS.** 사용자가 탐색기에서 한글을 직접 띄워
      문서를 열어둔 상태에서 변환기를 별도 프로세스로 3회 실행했다. 사용자 문서 본문
      1915자가 전후 동일, 창·PID 그대로, `Hwp.exe` 누수 0.
      단, 조건이 하나 있다: **변환기가 사용자의 한글과 다른 프로세스여야 한다.** 같은
      파이썬 프로세스 안에서 COM 으로 한글을 띄워 두면 `new=True` 여도 pyhwpx 가 같은
      인스턴스에 붙고, 우리 `quit()` 이 그 세션을 끊는다(실측). 배포 형태(exe = 자기 프로세스)
      에서는 해당 없지만, 이 코드를 라이브러리로 끌어다 쓸 때는 함정이다.
- [x] **공백·괄호가 든 파일명** (`어린이집 안내문 (3월).hwp`) — **PASS.** 변환 성공하고
      탐색기가 결과 PNG 를 선택한 채로 열린다(스크린샷 확인). 문자열 인자 형태 유지가 맞다.
- [~] **암호 걸린 hwp / 문서 복구 프롬프트가 뜨는 hwp** — **부분 확인.**
      - 손상된 파일(랜덤 바이트 + `.hwp`): 무한 대기 없음, 6.3초에 끝남. 다만 에러가 아니라
        *변환 성공* 으로 처리되어 알아볼 수 없는 PNG 가 나온다(치명적이진 않음).
      - 문서 복구 프롬프트가 걸린 상태(한글 강제 종료 후): 4.0초에 정상 변환. 복구 모달은
        한글을 UI 로 띄울 때만 뜨고 COM 자동화 경로에는 안 뜬다.
      - **암호 걸린 hwp 는 확인하지 못했다.** 이 한글 버전에서 `FileSaveAsDRM` 이 무동작이라
        테스트용 암호 문서를 만들 방법이 없었다. 실제 암호 문서를 한 건 구하면 그때 확인할 것.
        타임아웃/강제종료 전략은 여전히 미구현.
- [x] **`--collect-all pyhwpx` 로 빌드한 exe** — `FilePathCheckerModule.dll` 이
      `pyhwpx\FilePathCheckerModule.dll` 로 번들된 것을 PKG TOC 에서 확인했고, exe 실행 시
      개발 PC 의 venv 경로가 아니라 번들 경로(`_MEIxxxx\pyhwpx\...`)가 등록되는 것까지 확인했다.
      *다른 계정/PC 실행* 은 이 장비에서 할 수 없어 남아 있다.

#### ★COM 타임아웃/프로세스 격리 (`watchdog.py`, 2026-08-18 추가 — Windows 실기기 미검증)

바로 위 "타임아웃/강제종료 전략은 여전히 미구현" 항목에 대한 구현이다. `cli.process_file()`
전체를 `multiprocessing`(spawn) 자식 프로세스에서 돌리고, 부모가 타임아웃을 감시하다가
안 끝나면 자식과 자식이 새로 띄운 Hwp.exe(before/after PID diff 로 식별)를 강제 종료한다.
Mac 이라 실제 COM/한글 동작으로는 검증 못 했다 — `multiprocessing.get_context("fork")` 로
watchdog 의 타임아웃·킬·에러전달 로직 자체는 실제 OS 프로세스로 검증했지만(`tests/test_watchdog.py`),
**spawn 고유의 동작과 실제 Hwp.exe 상호작용은 이걸로 검증되지 않는다.** codex 크로스모델
리뷰(2회, gpt-5.6-sol xhigh) 로 BLOCKER 1건("어머니의 기존 한글까지 오인해서 죽일 수 있는"
PID 스냅샷 실패 케이스) + HIGH 1건(크래시 시 PID 정리 누락) + MEDIUM 2건을 잡아 전부 수정,
2차 리뷰까지 받았다. **Jell 결정: 지금 고치되 배포는 보류** — 아래 항목이 전부 끝나기 전에는
어머니 PC 에 올리지 않는다.

- [ ] **어머니가 이미 한글을 띄워둔 상태에서, 별도로 암호 걸린(또는 무한정 멈추는) hwp 를
      변환기에 드롭** — 타임아웃 후에도 어머니가 먼저 띄운 한글 창·문서가 그대로 살아있는지
      **PID 로 직접 확인**(작업 관리자에서 Hwp.exe PID 비교). 이게 이 기능 전체의 하드 제약이다.
- [ ] **실제 암호 걸린 hwp 로 변환 시도** — `DEFAULT_TIMEOUT_SECONDS`(30초) 안에 우리가 만든
      안내창("이 파일은 변환할 수 없어요. 암호가 걸려 있거나...")이 뜨는지, 그 사이 화면이
      완전히 먹통이 되지는 않는지(작업 표시줄 등 나머지 OS 는 정상 동작해야 한다).
- [ ] **타임아웃 후 작업 관리자에서 Hwp.exe 프로세스가 실제로 사라지는지** — 우리가 새로
      띄운 인스턴스가 좀비로 안 남는지.
- [ ] **`multiprocessing.freeze_support()` 가 onefile exe 에서 실제로 자식 프로세스 spawn 을
      가능하게 하는지** — codex 2차 리뷰가 "최신 PyInstaller 는 spawn worker 가 이미 풀린
      `_MEIxxxx` 를 재사용한다"고 확인했지만 이 exe 로 직접 검증한 적은 없다. 자식이
      `python.exe` 를 새로 찾으려다 실패하는 형태의 오류가 없는지 볼 것.
- [ ] **큰 문서(페이지 많은 문서) 기준 `timeout=30` 초가 충분한지** — 정상 변환인데
      타임아웃으로 오판되면 그것도 실패다. 정상 변환 시간이 30초에 근접하면 값을 올릴 것.
- [ ] **대용량 문서에서도 정상 변환이 30초 안에 여유 있게 끝나는지** (위 항목과 대칭 확인).
